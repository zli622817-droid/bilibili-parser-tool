# v1.0.0

B站解析工具 · 桌面版（普通版 / 登录版 / Cookie 提取工具）

> 仅供个人学习使用，请尊重版权，下载内容请在 24 小时内删除。

## ✨ 更新内容
- 支持链接 / BV 号解析，显示标题与全部可用清晰度
- 音视频分开下载，调用 ffmpeg 自动合并为 MP4，并清理临时文件
- 实时进度条、下载速度、已下载/总大小、运行日志
- 自定义保存路径（默认桌面），记忆保存位置与窗口大小/位置
- 登录版支持粘贴 Cookie，解锁 1080P 高码率 / 60帧 / 4K
- 新增「检测 Cookie 有效性」（校验是否登录、是否大会员）
- 圆角卡片 UI、DPI 自适应、无边框窗口

## 📦 文件（从本 Release 附件下载）
| 文件 | 说明 |
| --- | --- |
| `B站解析工具安装包.exe` | 二合一安装包（普通版 + 登录版，共享依赖），Inno Setup 现代向导，含卸载 |
| `登录版安装包.exe` | 仅登录版安装包 |
| `登录版自解压.exe` | 双击静默解压到同目录，无需安装 |
| `Cookie提取工具.zip` | 一键抓取浏览器 Cookie（Playwright） |
| `B站解析工具引导.zip` | Tauri 版 HTML 安装向导（解压即用） |

## 🚀 使用
- 源码运行：需 Python 3.9+，`pip install yt-dlp`，见 `README.md`
- 安装包：双击安装，装到 `C:\Program Files\B站解析工具`
- 解锁 4K：登录（需大会员）→ 用「Cookie提取工具」抓取 → 登录版自动读取 → 检测有效 → 解析

## ⚠️ 注意
- 免登录只能下公开清晰度；1080P 高码率 / 60帧 / 4K 需**大会员**账号并携带 Cookie。
- Cookie 仅保存在本机，不会上传，也不会打入安装包。
- 若杀软误报（PyInstaller / ffmpeg 未签名），点“允许/信任”即可，属误报。

## 🛠 源码与打包
- 源码：本仓库（普通版/登录版/Cookie 提取）
- 安装包由 Inno Setup 6 制作（`*.iss`），见 `README.md`
